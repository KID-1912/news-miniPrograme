<!--index.wxml-->
<view class="container">
  <view wx:if="{{isAuthenticated}}" a="123">
    <view><text>uid：{{authentication.uid}}</text></view>
  </view>
  <button wx:else type="primary" bind:tap="handleLogin">微信登录</button>
  <view> </view>
</view>